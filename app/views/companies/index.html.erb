
		<%= link_to 'Home', '# '%></p></li>
		<%= link_to 'Mandate' , mandates_path %>
		<%= link_to 'Interview' , interviews_path %>
		<%= link_to 'Applicant' , applicants_path %>
		<%= link_to 'Comment' , comments_path %>
		<%= link_to 'Company' , companies_path %>
<h1>Listing companies</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Domain</th>
    <th>No_Of_Employee</th>
    <th>Iso_Standard</th>
    <th>CMMI_level</th>
    <th>No_Of_Branch</th>
    <th>Email</th>
    <th>Phone_No</th>
    <th>Website</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @companies.each do |company| %>
  <tr>
    <td><%= company.name %></td>
    <td><%= company.address %></td>
    <td><%= company.domain %></td>
    <td><%= company.no_of_employee %></td>
    <td><%= company.iso_standard %></td>
    <td><%= company.cmmi_level %></td>
    <td><%= company.no_of_branch %></td>
    <td><%= company.email %></td>
    <td><%= company.phone_no %></td>
    <td><%= company.website %></td>
    <td><%= link_to 'Show', company %></td>
    <td><%= link_to 'Edit', edit_company_path(company) %></td>
    <td><%= link_to 'Destroy', company, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Company', new_company_path %>
 <%= gmaps4rails(@json) %>
